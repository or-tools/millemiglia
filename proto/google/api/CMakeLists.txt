INCLUDE(FindProtobuf)
FIND_PACKAGE(Protobuf REQUIRED)

INCLUDE_DIRECTORIES(${PROTOBUF_INCLUDE_DIR})
PROTOBUF_GENERATE_CPP(API_SRC API_HEADER resource.proto field_behavior.proto)
ADD_LIBRARY(api ${API_HEADER} ${API_SRC})
