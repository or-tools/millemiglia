INCLUDE(FindProtobuf)
FIND_PACKAGE(Protobuf REQUIRED)

INCLUDE_DIRECTORIES(${PROTOBUF_INCLUDE_DIR})
PROTOBUF_GENERATE_CPP(TYPE_SRC TYPE_HEADER datetime.proto latlng.proto)
ADD_LIBRARY(type ${TYPE_HEADER} ${TYPE_SRC})
