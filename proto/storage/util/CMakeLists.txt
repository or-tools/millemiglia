INCLUDE(FindProtobuf)
FIND_PACKAGE(Protobuf REQUIRED)

INCLUDE_DIRECTORIES(${PROTOBUF_INCLUDE_DIR})
PROTOBUF_GENERATE_CPP(UTIL_SRC UTIL_HEADER int128.proto)
ADD_LIBRARY(util ${UTIL_HEADER} ${UTIL_SRC})
